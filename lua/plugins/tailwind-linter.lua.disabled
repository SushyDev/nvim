return {
    {
        dir = '/Users/sushy/Documents/Projects/nvim/tailwind-linter.nvim',
        config = function ()
            require('tailwind-linter').setup({
                message = "Tailwind classes are not in the correct order.",
                languages = { "html", "php", "astro" },
            })
        end
    }
}
